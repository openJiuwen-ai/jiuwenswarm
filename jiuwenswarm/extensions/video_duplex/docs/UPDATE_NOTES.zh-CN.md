# 全双工文档更新说明

## 2026-09-24：对齐当前配置界面与持久化任务实现

### 版本依据

本次核对的是本地 `0.2.7` 合并 `Xiangyu` 后运行的版本，
不是对上游正式发布版本的声明。

| 项目 | 记录 |
|---|---|
| 本地分支 | `codex/merge-xiangyu-0.2.7` |
| 合并提交 | `3149889636fc48e7d20e19dd1555d228a4f804b2` |
| 0.2.7 基础提交 | `be0ae7ac40c1c6fe5d59b05ac011815d8caf718b` |
| Xiangyu 合入提交 | `1fbf6163ad6733b6ea535201dcf3b914faac918a` |
| 截图日期 | 2026-09-24 |
| 截图环境 | 本地运行的 Web 前端，中文界面 |

### 本次文档变更

- 明确配置入口为“设置 → 实验功能 → 任务对话全双工”。
- 增加 4 张实际界面截图：实验功能总览、Qwen 配置、JoyAI WebSocket 配置、
  JoyAI OpenAI HTTP 配置。
- 补充两类模型通道的字段、环境变量映射和配置示例。
- 区分任务全双工开关、独立语音转写开关与插件级环境开关。
- 说明保存、取消、密钥留空保留、模型卡片不代表连接成功等行为。
- 提醒切换语音协议不会自动改写 ASR/TTS 地址。
- 将主要使用步骤更新为任务输入框的全双工入口。
- 修正“刷新后任务不恢复”的旧描述，说明 SQLite 持久化、按对话恢复及重启后的
  `unknown` 状态；明确恢复记录不等于恢复媒体连接或自动重放工具。
- 更新任务依赖限制、诊断日志和代码入口。

本次变更只涉及文档与图片，没有修改运行代码，没有提交或推送 Git 变更。

### 已知限制：删除配置

当前前端 `VideoDuplexModelSettings.tsx` 的删除请求会清空所有模型/语音字段，
其中 `voice_protocol` 被置为空字符串；后端 `settings.py` 的校验仅接受
`native_ws` 或 `openai_http`。这两处实现存在冲突，删除可能返回
`voice_protocol must be native_ws or openai_http`。

该结论来自代码核对；为了保留现有配置，本次没有实际点击确认删除。
本文不将该问题描述为已修复。临时隐藏任务入口可关闭实验功能开关，
这不会清除密钥。

### 截图说明

图片索引与重新采集要求见 [截图说明](images/README.md)。

截图保留真实布局；密钥由密码输入框或星号占位遮蔽。
租户 WebSocket 地址以及 HTTP 演示字段在未保存草稿中换成示例值，
截图后已取消。没有保存这些演示字段，也没有改变开关。

图中 Qwen 模型 `qwen3.8-omni-flash-realtime`、音色 `Tina` 是该实例已有配置，
不表示代码默认值或服务商当前通用推荐值。

### 核对范围

已核对当前页面入口、两种模型通道、两种 JoyAI 语音协议和配置字段显示；
已阅读设置持久化、任务恢复和任务服务的相关代码。

本次没有启动屏幕/麦克风会话，没有进行上游模型连通性或音频端到端验证，
没有运行功能测试。用户操作验证步骤和测试命令见 [使用文档](README.zh-CN.md)。
