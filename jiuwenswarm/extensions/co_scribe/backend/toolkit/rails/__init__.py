"""The rails: what a proposed edit has to survive before it becomes a write.

Locating the quoted span (``textmap``), judging and applying the proposal
(``range_rail``), and checking the result rather than only the boundary
(``result_predicates``).
"""
