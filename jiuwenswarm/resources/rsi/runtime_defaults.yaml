# RSI-only defaults. Model identity and credentials come from the user's selection.
# Context windows and timeouts are preserved. RSI leaves output limits to the provider.
# Missing context capacities use Core's model metadata.
rsi_optimizer_roles: [analysis, member_optimization, judge]
timeout: 900
