# JiuwenClaw AgentServer 精简依赖配置
# 用于鸿蒙等受限编译环境
#
# 使用方式：
#   pip install -r requirements-minimal.txt
#
# 注意：
# - 此文件排除了渠道适配器（飞书、钉钉、Telegram 等）
# - 排除了向量存储（chromadb, pymilvus, sqlite-vec）
# - 排除了重型文档处理（tree-sitter）；pdfplumber 为 OfficeAce 导出/预览链所需，
#   纯 Python wheel（py3-none-any）无鸿蒙编译问题，已预置于 wheels/ 并列入下方依赖（2026-09-08）
# - 排除了测试依赖（pytest 系列）
#
# 如需本地 openjiuwen，设置环境变量：
#   USE_LOCAL_OPENJIUWEN=1
#   AGENT_CORE_PATH=/path/to/agent-core

# ============================================================
# 核心 HTTP 和网络依赖
# ============================================================
aiohttp>=3.11.12
requests>=2.32.3
httpx>=0.24.0
websockets>=12.0

# ============================================================
# 异步 IO 和文件处理
# ============================================================
aiofiles>=25.1.0
anyio>=4.0.0
aiosqlite>=0.22.1
jieba>=0.42
watchdog>=6.0.0

# ============================================================
# 数据库和 ORM
# ============================================================
sqlalchemy>=2.0.41
sqlmodel>=0.0.37
alembic>=1.13.1
greenlet>=3.3.2

# ============================================================
# 数据验证和序列化
# ============================================================
pydantic>=2.11.0
pydantic-core>=2.46.0
ruamel.yaml>=0.18.0
json-repair>=0.58.0
# JiuwenClaw core runtime dependencies added in the main-branch merge.
latex2mathml>=3.81.0
mathml2omml==0.0.2
markdown>=3.8
markdown-it-py>=4.0.0,<5.0.0
python-docx>=1.2.0
pyyaml>=6.0
fastapi>=0.115
mcp>=1.27.2,<2

# ============================================================
# Office 文档处理与 markdown 渲染（OfficeAce 导出/预览链）
# ============================================================
# 纯 Python wheel（py3-none-any），均已预置于 wheels/ 目录；
# 版本与 OfficeAce augment_hnp_deps.py 注入链验证过的版本一致，勿随意升级。
# - python-pptx / XlsxWriter：PPT、Excel 生成
# - pdfplumber / pdfminer.six：PDF 解析（提取文本/表格）
# - linkify-it-py / mdit-py-plugins / uc-micro-py：markdown-it-py 生态配套，
#   markdown_it 开 linkify/插件选项时按需 import
python-pptx==1.0.2
XlsxWriter==3.2.9
pdfplumber==0.11.6
pdfminer.six==20250327
linkify-it-py==2.1.0
mdit-py-plugins==0.6.1
uc-micro-py==2.0.0

# ============================================================
# 时间和调度
# ============================================================
python-dateutil>=2.9.0.post0
croniter>=2.0.0
# OhOS 无系统 IANA 时区库；ZoneInfo("Asia/Shanghai") 等需 tzdata
tzdata>=2024.1

# ============================================================
# 日志和监控
# ============================================================
loguru>=0.7.3
python-json-logger
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
# Agent-core enterprise-dev imports these modules while loading team observability.
# Keep them in the minimal OHOS dependency closure so a fresh venv is packable.
opentelemetry-exporter-otlp-proto-common>=1.25.0
opentelemetry-proto>=1.25.0
protobuf>=5.0.0,<8

# ============================================================
# 文件锁和并发
# ============================================================
filelock>=3.20.1
portalocker>=3.2.0
tenacity>=9.1.2

# ============================================================
# 环境和配置
# ============================================================
python-dotenv>=1.2.1
python-multipart>=0.0.27

# ============================================================
# 加密和编码
# ============================================================
pycryptodome>=3.23.0
charset-normalizer>=3.4.4

# ============================================================
# OpenAI SDK（LLM 调用）
# ============================================================
openai>=1.108.0

# ============================================================
# Token 计算（可选 - 如有 .so 权限问题可跳过）
# ============================================================
tiktoken>=0.12.0

# ============================================================
# 其他核心依赖
# ============================================================
# openjiuwen retrieval callbacks imports tqdm during AgentServer startup
tqdm>=4.66.0
numpy>=1.26.0
beautifulsoup4>=4.12.0
docx2txt>=0.8

# ============================================================
# 网页内容抽取（默认 WebFetch 工具依赖）
# ============================================================
trafilatura>=1.6.0,<2
# lxml 5.2+ 将 html.clean 拆出；trafilatura/justext 仍依赖 lxml.html.clean
# 1.x 要求 lxml>=5.2.2，可命中本地预编 wheel (lxml-6.1.0)；
# 2.x 强制 lxml>=6.1.1 会触发源码编译（鸿蒙缺 libxml2 dev，失败）
# lxml_html_clean 0.4.5 要求 lxml>=6.1.1；锁定到 0.4.4 以兼容本地 lxml 6.1.0 预编 wheel
lxml>=6.1.0,<6.1.1
lxml_html_clean>=0.4.0,<0.4.5

# ============================================================
# openjiuwen 核心 Agent 运行时
# ============================================================
# 方式 1：从 gitcode 安装（需要网络）
openjiuwen-harmonyos @ git+https://gitcode.com/openJiuwen/agent-core.git@enterprise-dev#subdirectory=harmonyos

# 方式 2：从本地路径安装（鸿蒙环境推荐）
# 设置 USE_LOCAL_OPENJIUWEN=1 和 AGENT_CORE_PATH 环境变量

# ============================================================
# jiuwenclaw 本体（从当前目录安装）
# ============================================================
# pip install -e . --no-deps  # 使用此命令跳过主依赖
