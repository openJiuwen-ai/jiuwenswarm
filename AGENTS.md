# External Project Knowledge Base

For JiuwenSwarm Coding Agent or Code Graph work, the authoritative long-term Project KB is external:

`/Users/mmmmroy/Downloads/jiuwenswarm-coding-agent-kb`

Before substantial work:

1. Read the external KB `AGENTS.md`.
2. Read `00_INDEX.md` as a router and `10_handoff/CURRENT.md` for current state.
3. Read only knowledge relevant to the current task; do not load the whole KB by default.
4. Verify current code/tests/artifacts when available. Evidence outranks stale KB claims, and meaningful conflicts should be corrected in the KB.

Maintenance boundary:

- Repository `docs/ai/` is an independent Cursor development-memory system for experiments, problems, trajectories, QA, phases and handoff. Do not redirect or replace it. The external Downloads KB is a separate long-term knowledge system.
- Consolidate durable Activity/Knowledge/Evolution/Learning only at meaningful milestones, with deduplication and provenance.
- Do not save raw chat/session dumps.
- Separate Observation, Interpretation and Decision; also separate user-confirmed work from Agent suggestions or uncertain attribution.
- Preserve factual raw material for later review, but do not proactively generate interview scripts, STAR stories or resume bullets unless the user explicitly requests them.
