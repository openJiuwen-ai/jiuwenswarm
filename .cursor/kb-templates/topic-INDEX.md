# 主题：<短名>

一句话：这个目录记什么、不记什么。

打开主题时先读本页，不要从文件名列表瞎点。

| 步 | 读什么 |
|----|--------|
| 1 | `current/` 现行口径 |
| 2 | `SUMMARY.md` / `QA.md` |
| 3 | 需要某天证据才进 `runs/YYYY-MM-DD-slug/` |
| 4 | `archive/` 默认不读 |

主题根只留 `INDEX.md`、`SUMMARY.md`、`EVOLUTION.md`、`QA.md`。  
现行稿进 `current/`，一次性质证进带日期的 `runs/`，被替代的进 `archive/`（先标再删）。
